server:
    port: 4050

spring:
  application:
    name: leasing-core-service
  profiles:
    active: localhost

---
#localhost
spring:
  profiles: localhost
  cloud:
    config:
      uri: http://localhost:8889/